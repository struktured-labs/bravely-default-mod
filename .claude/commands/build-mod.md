Build the BravelyMod C# project and deploy it to the game's Mods folder.

Steps:
1. Run `dotnet build BravelyMod/` from the project root
2. Verify the build succeeded (0 errors)
3. Confirm `~/.steam/debian-installation/steamapps/common/BDFFHD/Mods/BravelyMod.dll` exists and show its timestamp
4. If build fails, diagnose errors and fix them
5. Report build status
